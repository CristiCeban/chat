import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    avatarContainer: {
        borderRadius: 150 / 2,
        alignItems: "center",
        justifyContent: "center"
    },
    letters: {
        color: "white",
        fontWeight: "bold"
    },
    avatarImage: {
        resizeMode: "cover",
    }
})
