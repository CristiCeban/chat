import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        display: 'flex',
        flexDirection: 'row',
        minHeight: 60,
        justifyContent: 'flex-start',
        alignItems: 'center',
    },
    name: {
        fontSize: 16,
        marginLeft: 10
    }
})
